# Your portfolio site

Open `index.html` in a browser to preview it. Edit the placeholder text in `index.html` to add your name, role, experience, project details, email, social links, and résumé link.

The three project cards are deliberately graphic placeholders; replace their `href="#"` links with project pages, case studies, or external work. To publish, upload this folder to GitHub Pages, Netlify, or Vercel.
